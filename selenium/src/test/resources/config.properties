base.url=http://localhost:3000

browser=chrome
headless=false

# Generous because every test drives a real application against a real
# database: the first test in a run also pays Chrome start-up and
# connection-pool warm-up.
explicit.wait.seconds=30
page.load.timeout.seconds=60

invalid.username=invalid.user@novabank.test
invalid.password=InvalidPassword123!
